- model_class = MysqlAudio
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:name) + ':'
  %br
  = @mysql_audio.name
%p
  %strong= model_class.human_attribute_name(:attachment) + ':'
  %br
  = @mysql_audio.attachment

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), mysql_audios_path, :class => 'btn'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_mysql_audio_path(@mysql_audio), :class => 'btn'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), mysql_audio_path(@mysql_audio), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
